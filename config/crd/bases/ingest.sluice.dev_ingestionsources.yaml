---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ingestionsources.ingest.sluice.dev
spec:
  group: ingest.sluice.dev
  names:
    categories:
    - sluice
    kind: IngestionSource
    listKind: IngestionSourceList
    plural: ingestionsources
    shortNames:
    - isrc
    - isrcs
    singular: ingestionsource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalRowsIngested
      name: Rows
      type: integer
    - jsonPath: .status.lastRun.result
      name: Last Run
      type: string
    - jsonPath: .status.watermark
      name: Watermark
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IngestionSource declares a vendor API to pull on a schedule and a table to
          land the records in. The controller owns a CronJob for the schedule, a Job
          for any requested backfill, and the watermark that makes runs incremental.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IngestionSource
            properties:
              backfill:
                description: backfill requests a one-off historical pull.
                properties:
                  from:
                    description: from is the watermark value to start at.
                    type: string
                  requestID:
                    description: |-
                      requestID identifies this backfill. Changing it starts a new one; the
                      controller records the last completed ID so an unchanged spec never
                      re-runs a backfill.
                    minLength: 1
                    type: string
                  to:
                    description: to is the watermark value to stop at.
                    type: string
                required:
                - requestID
                type: object
              concurrencyPolicy:
                default: Forbid
                description: |-
                  concurrencyPolicy controls what happens when a run is still going at the
                  next scheduled time. Forbid is the default because two concurrent runs
                  share one watermark and will duplicate rows.
                enum:
                - Allow
                - Forbid
                - Replace
                type: string
              destination:
                description: destination describes where records land.
                properties:
                  bigQuery:
                    description: bigQuery configures the BigQuery sink.
                    properties:
                      createIfMissing:
                        default: true
                        description: |-
                          createIfMissing creates the table from the observed record shape when it
                          does not exist.
                        type: boolean
                      dataset:
                        description: dataset to write into.
                        minLength: 1
                        type: string
                      deadLetterTable:
                        description: |-
                          deadLetterTable receives records that could not be written, along with
                          the reason. Without one, rejected records are counted and dropped.
                        type: string
                      location:
                        description: location is the BigQuery dataset location, for
                          example "US".
                        type: string
                      projectID:
                        description: projectID owning the dataset.
                        minLength: 1
                        type: string
                      table:
                        description: table to write into.
                        minLength: 1
                        type: string
                      writeDisposition:
                        default: Append
                        description: writeDisposition controls whether a run appends
                          to or replaces the table.
                        enum:
                        - Append
                        - Truncate
                        type: string
                    required:
                    - dataset
                    - projectID
                    - table
                    type: object
                  type:
                    default: Stdout
                    description: type names the sink.
                    enum:
                    - BigQuery
                    - Stdout
                    type: string
                type: object
                x-kubernetes-validations:
                - message: bigQuery must be set when type is BigQuery
                  rule: self.type != 'BigQuery' || has(self.bigQuery)
              failedRunHistoryLimit:
                default: 3
                description: failedRunHistoryLimit is how many failed run Jobs to
                  keep for debugging.
                format: int32
                minimum: 0
                type: integer
              failurePolicy:
                description: failurePolicy controls retry and give-up behavior.
                properties:
                  backoffLimit:
                    default: 3
                    description: backoffLimit is the number of pod retries within
                      a single run.
                    format: int32
                    minimum: 0
                    type: integer
                  suspendAfterConsecutiveFailures:
                    default: 5
                    description: |-
                      suspendAfterConsecutiveFailures stops scheduling once this many runs
                      have failed in a row, so a broken source does not page someone every
                      night for a week. Zero disables the behavior.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              runner:
                description: runner describes the pod that performs a run.
                properties:
                  activeDeadlineSeconds:
                    description: activeDeadlineSeconds bounds the wall time of a single
                      run.
                    format: int64
                    type: integer
                  env:
                    description: env holds extra environment variables for the worker
                      container.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  image:
                    description: |-
                      image is the worker image. Defaults to the operator's own image, which
                      ships the worker as a subcommand.
                    type: string
                  imagePullPolicy:
                    description: imagePullPolicy for the worker container.
                    type: string
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: nodeSelector for the run pod.
                    type: object
                  resources:
                    description: resources for the worker container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serviceAccountName:
                    description: |-
                      serviceAccountName the run executes as. On GKE this is the account bound
                      to a Google service account through Workload Identity.
                    type: string
                  tolerations:
                    description: tolerations for the run pod.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              schedule:
                description: |-
                  schedule is a cron expression in the cluster's timezone, matching
                  CronJob semantics.
                minLength: 1
                type: string
              schema:
                description: schema controls the reaction to record shape changes.
                properties:
                  drift:
                    default: Warn
                    description: drift selects the reaction to a change in record
                      shape.
                    enum:
                    - Ignore
                    - Warn
                    - EvolveAdditive
                    - Fail
                    type: string
                type: object
              source:
                description: source describes the upstream system.
                properties:
                  http:
                    description: http configures an HTTP source.
                    properties:
                      auth:
                        description: auth selects the credential scheme.
                        properties:
                          headerName:
                            description: headerName is the header the credential is
                              sent in when type is Header.
                            type: string
                          secretRef:
                            description: |-
                              secretRef holds the credential. For Bearer and Header it is the token.
                              For Basic the referenced Secret must carry "username" and "password"
                              keys and the key field is ignored.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          type:
                            default: None
                            description: type selects the credential scheme.
                            enum:
                            - None
                            - Bearer
                            - Header
                            - Basic
                            type: string
                        type: object
                      body:
                        description: body is a literal request body used when method
                          is POST.
                        type: string
                      headers:
                        description: headers are sent on every request.
                        items:
                          description: HeaderValue is a request header whose value
                            may come from a Secret.
                          properties:
                            name:
                              description: name is the HTTP header name.
                              minLength: 1
                              type: string
                            value:
                              description: |-
                                value is a literal header value. Do not put credentials here; use
                                valueFrom so the value stays in a Secret.
                              type: string
                            valueFrom:
                              description: valueFrom reads the header value from a
                                Secret key.
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      maxRequestsPerSecond:
                        description: |-
                          maxRequestsPerSecond throttles the worker so a nightly pull does not
                          trip an upstream rate limit. Zero means unthrottled.
                        format: int32
                        minimum: 0
                        type: integer
                      method:
                        default: GET
                        description: method is the HTTP method used for each request.
                        enum:
                        - GET
                        - POST
                        type: string
                      pagination:
                        description: pagination describes how to walk multi-response
                          result sets.
                        properties:
                          cursorParam:
                            description: cursorParam is the query parameter the cursor
                              is sent back in.
                            type: string
                          maxPages:
                            default: 1000
                            description: |-
                              maxPages caps how many responses a single run will read. It is a
                              circuit breaker against an upstream that never signals the end of the
                              result set, which is a real failure mode and not a theoretical one.
                            format: int32
                            minimum: 1
                            type: integer
                          nextCursorPath:
                            description: |-
                              nextCursorPath is a dotted path into the response body holding the next
                              cursor, for example "meta.next_cursor". Required when type is Cursor.
                            type: string
                          pageParam:
                            description: pageParam is the query parameter carrying
                              the page number or offset.
                            type: string
                          pageSize:
                            default: 100
                            description: pageSize is how many records to request per
                              page.
                            format: int32
                            minimum: 1
                            type: integer
                          sizeParam:
                            description: sizeParam is the query parameter carrying
                              the page size.
                            type: string
                          startPage:
                            description: startPage is the first page number or offset
                              to request.
                            format: int32
                            minimum: 0
                            type: integer
                          type:
                            default: None
                            description: type selects the pagination strategy.
                            enum:
                            - None
                            - Cursor
                            - PageNumber
                            - Offset
                            - LinkHeader
                            type: string
                        type: object
                      query:
                        additionalProperties:
                          type: string
                        description: query holds static query parameters sent on every
                          request.
                        type: object
                      recordsPath:
                        description: |-
                          recordsPath is a dotted path to the array of records in each response,
                          for example "data.items". Empty means the response body is itself the
                          array.
                        type: string
                      timeout:
                        default: 30s
                        description: timeout bounds a single HTTP request.
                        type: string
                      url:
                        description: url is the endpoint to pull from.
                        pattern: ^https?://
                        type: string
                    required:
                    - url
                    type: object
                  type:
                    default: HTTP
                    description: type names the kind of upstream.
                    enum:
                    - HTTP
                    type: string
                type: object
                x-kubernetes-validations:
                - message: http must be set when type is HTTP
                  rule: self.type != 'HTTP' || has(self.http)
              startingDeadlineSeconds:
                description: startingDeadlineSeconds is how late a missed run may
                  still start.
                format: int64
                type: integer
              successfulRunHistoryLimit:
                default: 3
                description: successfulRunHistoryLimit is how many completed run Jobs
                  to keep.
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: |-
                  suspend stops new runs from being scheduled without deleting the
                  recorded watermark, so ingestion resumes where it left off.
                type: boolean
              watermark:
                description: watermark makes runs incremental. Without it every run
                  is a full pull.
                properties:
                  format:
                    default: RFC3339
                    description: format describes how to parse and order watermark
                      values.
                    enum:
                    - RFC3339
                    - UnixSeconds
                    - Opaque
                    type: string
                  initialValue:
                    description: initialValue is used on the first run, before any
                      watermark is stored.
                    type: string
                  param:
                    description: |-
                      param is the query parameter used to request records at or after the
                      stored watermark.
                    minLength: 1
                    type: string
                  recordField:
                    description: |-
                      recordField is the field on each record holding the watermark value,
                      for example "updated_at".
                    minLength: 1
                    type: string
                  toParam:
                    description: |-
                      toParam is an optional query parameter carrying an upper bound. It is
                      used only by backfills, which pull a closed range rather than everything
                      since the last run.
                    type: string
                required:
                - param
                - recordField
                type: object
            required:
            - destination
            - schedule
            - source
            type: object
          status:
            description: status defines the observed state of IngestionSource
            properties:
              conditions:
                description: conditions represent the current state of the IngestionSource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveFailures:
                description: consecutiveFailures counts runs that have failed in a
                  row.
                format: int32
                type: integer
              cronJobName:
                description: cronJobName is the owned CronJob driving the schedule.
                type: string
              lastCompletedBackfillID:
                description: |-
                  lastCompletedBackfillID is the backfill request the controller has
                  already satisfied.
                type: string
              lastRun:
                description: lastRun is the most recent run, successful or not.
                properties:
                  backfillID:
                    description: backfillID is set when the run was a backfill rather
                      than a scheduled pull.
                    type: string
                  completionTime:
                    description: completionTime is when the run finished.
                    format: date-time
                    type: string
                  jobName:
                    description: jobName is the Job that performed the run.
                    type: string
                  message:
                    description: message explains a failure.
                    type: string
                  requestCount:
                    description: requestCount is how many upstream requests the run
                      made.
                    format: int32
                    type: integer
                  result:
                    description: result is the outcome of the run.
                    enum:
                    - Running
                    - Succeeded
                    - Failed
                    type: string
                  rowsIngested:
                    description: rowsIngested is how many records the run wrote.
                    format: int64
                    type: integer
                  rowsRejected:
                    description: rowsRejected is how many records the run could not
                      write.
                    format: int64
                    type: integer
                  schemaFingerprint:
                    description: schemaFingerprint is a stable hash of the record
                      shape the run saw.
                    type: string
                  startTime:
                    description: startTime is when the run began.
                    format: date-time
                    type: string
                  watermark:
                    description: watermark is the highest watermark value the run
                      observed.
                    type: string
                type: object
              lastScheduleTime:
                description: lastScheduleTime is when a run was last started by the
                  schedule.
                format: date-time
                type: string
              lastSuccessfulRun:
                description: lastSuccessfulRun is the most recent run that completed.
                properties:
                  backfillID:
                    description: backfillID is set when the run was a backfill rather
                      than a scheduled pull.
                    type: string
                  completionTime:
                    description: completionTime is when the run finished.
                    format: date-time
                    type: string
                  jobName:
                    description: jobName is the Job that performed the run.
                    type: string
                  message:
                    description: message explains a failure.
                    type: string
                  requestCount:
                    description: requestCount is how many upstream requests the run
                      made.
                    format: int32
                    type: integer
                  result:
                    description: result is the outcome of the run.
                    enum:
                    - Running
                    - Succeeded
                    - Failed
                    type: string
                  rowsIngested:
                    description: rowsIngested is how many records the run wrote.
                    format: int64
                    type: integer
                  rowsRejected:
                    description: rowsRejected is how many records the run could not
                      write.
                    format: int64
                    type: integer
                  schemaFingerprint:
                    description: schemaFingerprint is a stable hash of the record
                      shape the run saw.
                    type: string
                  startTime:
                    description: startTime is when the run began.
                    format: date-time
                    type: string
                  watermark:
                    description: watermark is the highest watermark value the run
                      observed.
                    type: string
                type: object
              lastTriggerID:
                description: |-
                  lastTriggerID is the value of the trigger annotation the controller has
                  already started a run for, so the same trigger never runs twice.
                type: string
              observedGeneration:
                description: observedGeneration is the spec generation the controller
                  last acted on.
                format: int64
                type: integer
              phase:
                description: phase is a coarse rollup for humans reading kubectl output.
                enum:
                - Pending
                - Scheduled
                - Running
                - Suspended
                - Degraded
                type: string
              schemaFingerprint:
                description: schemaFingerprint is the record shape accepted by the
                  last run.
                type: string
              totalRowsIngested:
                description: totalRowsIngested is the cumulative count across all
                  runs.
                format: int64
                type: integer
              watermark:
                description: watermark is the value the next run will resume from.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
